# ========================================================
# Database Configuration (PostgreSQL / MySQL)
# ========================================================
# Type: postgresql or mysql
db.type=postgresql
db.host=localhost
db.port=5432
db.name=postgres
db.user=postgres
db.password=postgres
